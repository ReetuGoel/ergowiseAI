name: Azure Static Web Apps CI/CD (Disabled)

on:
  workflow_dispatch:  # Only run manually until Azure secret is configured

jobs:
  build_and_deploy_job:
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - name: Info
        run: |
          echo "Azure deployment disabled until secret is configured"
          echo "Use GitHub Pages deployment instead"
