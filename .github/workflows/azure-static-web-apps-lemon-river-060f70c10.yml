# Azure Static Web Apps workflow disabled
# This workflow is disabled because the Azure API token is not configured in this repository
# Use the GitHub Pages deployment instead: https://ReetuGoel.github.io/ergowiseAI

name: Azure Static Web Apps (Disabled)

on:
  workflow_dispatch:  # Only run manually if Azure is configured later

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Information
        run: |
          echo "⚠️  Azure Static Web Apps deployment is disabled"
          echo "🔧 Reason: Missing AZURE_STATIC_WEB_APPS_API_TOKEN secret"
          echo "✅ Alternative: Use GitHub Pages deployment"
          echo "🌐 Your site: https://ReetuGoel.github.io/ergowiseAI"
          echo ""
          echo "To enable Azure deployment:"
          echo "1. Create Azure Static Web App in Azure Portal"
          echo "2. Get the deployment token"
          echo "3. Add it as repository secret"
          echo "4. Re-enable this workflow"
